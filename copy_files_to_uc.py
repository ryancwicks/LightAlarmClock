#This script copies the required web interface files to the uC
import serial
import glob

def copy_file_to_server (file) {

}

if __name__=="__main__":

    import argparse

    parser = argparse.ArgumentParser(prog=__name__, description="A program for loading files onto a ESP32 uC.")

    parser.add